# Générer avec: openssl rand -hex 32
ENCRYPTION_KEY=

# Générer avec: node -e "const b=require('bcryptjs'); b.hash('VOTRE_MOT_DE_PASSE',12).then(console.log)"
ADMIN_PASSWORD_HASH=

# Générer avec: openssl rand -hex 32
JWT_SECRET=

# URL de base (production)
BASE_URL=https://votre-domaine.com
